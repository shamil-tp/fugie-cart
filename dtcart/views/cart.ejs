<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Cart</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/cart.css">
    <style>
        .modal.hidden {
            display: none;
        }

        .modal {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 999;
            backdrop-filter: blur(4px);
        }

        .modal-content {
            background: #fff;
            padding: 24px;
            border-radius: 16px;
            width: 90%;
            max-width: 320px;
            text-align: center;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            animation: modalSlide 0.3s ease-out;
        }

        @keyframes modalSlide {
            from { transform: translateY(20px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .modal-content h2 {
            margin-top: 0;
            margin-bottom: 8px;
            color: #1e293b;
            font-size: 1.25rem;
        }

        .modal-content p {
            color: #64748b;
            line-height: 1.5;
            margin-bottom: 24px;
        }

        .modal-content .primary-btn {
            width: 100%;
            border: none;
            padding: 12px;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: opacity 0.2s;
            background: #1e1e1e;
            color: white;
        }

        .modal-content .primary-btn:active {
            opacity: 0.8;
        }
    </style>
</head>
<body>
    <div class="mobile-container">
   <%- include('components/topBar', { user }) %>

        <header class="page-header">
  <button class="back-btn" onclick="window.location.href='/'">←</button>
  <h1>View Cart</h1>
</header>


        <div class="cart-items-list">
            <% if (typeof cartItems !== 'undefined' && cartItems.length > 0) { %>
                <% cartItems.forEach(function(item) { %>
                    <div class="cart-item">
                        <div class="cart-item-image">
                            <!-- Assuming item structure has image, name, quantity, price -->
                            <img src="<%= item.img || './images/pic_10.jpg' %>" alt="<%= item.name %>" onerror="this.src='./images/pic_10.jpg'">
                        </div>
                        <div class="cart-item-info">
                            <h3 class="cart-item-name"><%= item.name %></h3>
                            <div class="cart-item-metrics">
                                <span class="cart-item-qty">Qty: <%= item.quantity %></span>
                                <span class="cart-item-price">$<%= item.price.toFixed(2) %> each</span>
                            </div>
                        </div>
                        <div class="cart-item-total">
                            $<%= (item.price * item.quantity).toFixed(2) %>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="empty-cart-message" style="text-align: center; padding: 2rem;">
                    <p>Your cart is empty.</p>
                    <a href="/" class="primary-btn" style="display: inline-block; margin-top: 1rem; text-decoration: none;">Start Shopping</a>
                </div>
            <% } %>
        </div>

        <form class="cart-footer" action="/cart" method="post">
            <div class="grand-total-section">
                <span>Grand Total</span>
                <span class="grand-total-amount">$<%= (typeof total !== 'undefined' ? total : 0).toFixed(2) %></span>
            </div>
            <input type="number" hidden value="<%= (typeof total !== 'undefined' ? total : 0).toFixed(2) %>" name="amount">
            <button class="primary-btn checkout-btn" type="submit">Proceed to Buy</button>
        </form>

        <%- include('components/navbar', { page: 'cart' }) %>
    </div>
    
    <div id="emptyCartModal" class="modal hidden">
        <div class="modal-content">
            <div style="font-size: 3rem; margin-bottom: 12px;">🛒</div>
            <h2>Cart Empty</h2>
            <p>Your cart is empty. Please add some items before checking out!</p>
            <button class="primary-btn" onclick="document.getElementById('emptyCartModal').classList.add('hidden')">Got it</button>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const emptyCartError = <%- JSON.stringify(locals.emptyCartError || false) %>;
            if (emptyCartError) {
                document.getElementById('emptyCartModal').classList.remove('hidden');
            }
        });
    </script>
    
    <div class="desktop-message">
        <p>This application is only available on mobile devices.</p>
    </div>
</body>
</html>