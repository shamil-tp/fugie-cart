<style>
    .top-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.25rem;
}

.user-greeting .hello {
    font-size: 0.85rem;
    color: var(--text-secondary);
}

.user-greeting .username {
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--text-primary);
}

.coin-balance {
    display: flex;
    align-items: center;
    gap: 6px;
    background: #f9fafb;
    padding: 0.4rem 0.7rem;
    border-radius: 999px;
    border: 1px solid var(--border-color);
    font-weight: 600;
}

</style>

<header class="top-bar">
  <div class="user-greeting">
    <p class="hello">Hello,</p>
    <h1 class="username"><%= user?.name || 'Guest' %></h1>
  </div>

  <div class="coin-balance">
    <span class="coin-icon">🪙</span>
    <span class="coin-count"><%= user?.balance ?? 0 %></span>
  </div>
</header>


